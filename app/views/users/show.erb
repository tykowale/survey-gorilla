<!-- shows user profile, which has list of names of all their surveys, which can be clicked on to edit -->
<h2>Welcome, <%= @user.name %>!</h2>
<h3>My Surveys</h3>

<ul>
  <% @user.surveys.each do |survey| %>
    <%= survey.title %></a></li>
    <li><a href="/survey/<%= survey.id %>"><%= survey.title %></a></li>
  <% end %>
</ul>
